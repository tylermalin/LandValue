# Runtime dependencies (loosely pinned for broad wheel availability, incl.
# Streamlit Community Cloud). geopandas/shapely are intentionally omitted —
# spatial.py uses a pure-Python implementation, so they are not required.
streamlit>=1.30
pandas>=2.0
pydeck>=0.8
jinja2>=3.1
python-dotenv>=1.0
# Optional: only used in live mode (Apify ingestion) and PDF dossier export.
apify-client>=3.0
pdfkit>=1.0
